# Anant Portfolio

Advanced interactive portfolio built with Vite, Three.js, GSAP, and responsive CSS.

## Commands

```bash
npm install
npm run dev
npm run build
```

Update the placeholder project, experience, email, and copy in `index.html`.
